ScrollLock is a simple Chrome plugin that locks a scroll on desired pages.
Also compatible with mobile Chrome browsers. 
Personally I am using it on a Mises mobile browser and Chrome for a PC.

# TODO
* enable to enter the input on enter
* refresh the page after the record is added
* round the edges on main window
* deleting the record does not fit in the main window
* an link to github
